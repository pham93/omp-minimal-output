{
  "name": "@local/omp-minimal-output",
  "version": "0.3.0",
  "description": "Grok-build-style minimal console output for omp",
  "type": "module",
  "main": "index.ts",
  "pi": {
    "extensions": ["./index.ts"],
    "settings": {
      "opacity": {
        "type": "number",
        "default": 0.5,
        "min": 0,
        "max": 1,
        "step": 0.05,
        "description": "Row text opacity (0–1). Indicator stays full color."
      },
      "indicator": {
        "type": "enum",
        "values": ["diamond", "dot", "none"],
        "default": "diamond",
        "description": "Live/settled row mark: diamond (◈◉◎○/◆), dot (●○◉○/●), none (space)."
      },
      "indicatorAnimation": {
        "type": "boolean",
        "default": true,
        "description": "Cycle live indicator frames at 120ms. Off uses the first frame."
      },
      "nativeBash": { "type": "boolean", "default": false, "description": "Leave bash on the native Ohmypi renderer." },
      "nativeRead": { "type": "boolean", "default": false, "description": "Leave read on the native Ohmypi renderer (also disables the read-group skin)." },
      "nativeGrep": { "type": "boolean", "default": false, "description": "Leave grep on the native Ohmypi renderer." },
      "nativeGlob": { "type": "boolean", "default": false, "description": "Leave glob on the native Ohmypi renderer." },
      "nativeWrite": { "type": "boolean", "default": false, "description": "Leave write on the native Ohmypi renderer." },
      "nativeEdit": { "type": "boolean", "default": false, "description": "Leave edit on the native Ohmypi renderer." },
      "editShowTabs": { "type": "boolean", "default": true, "description": "Show tabs as → in edit diffs." },
      "editShowSpaces": { "type": "boolean", "default": false, "description": "Show spaces as · in edit diffs." }
    }
  },
  "files": ["index.ts", "filters.ts", "text.ts", "theme.ts", "results.ts", "loaders.ts", "edit-card.ts", "read-group.ts", "config.ts", "minimal-output.yml", "README.md", "LICENSE"],
  "keywords": ["pi", "pi-coding-agent", "pi-package", "extension", "minimal-output"],
  "license": "MIT",
  "peerDependencies": { "@oh-my-pi/pi-coding-agent": "*" }
}
